{
  "_comment": "Per-project config for scripts/readiness.py. See RELEASE_READINESS.md. || 1.0.1: field fixes #7/#8/#9 (panels W err-safe MEDIUM -> X clean) PLUS a polyglot generalization of test detection the maintainer asked for, driven to closure by panels Y-DD. The Y-DD line found and closed four successive CARDINAL test-class-liveness gaps (direct -> inherited/nested -> combined fixed point -> Python/tree-sitter is_test_file asymmetry); DD is clean on both models with an explicit convergence verdict that the class is closed. 1.0.1 ships on the maintainer's decision; the strict 2-consecutive-clean streak applies to the headline release line.",
  "_available_models_note": "sonnet (claude-sonnet-4-6) was restored to the panel process on 2026-06-24 after the maintainer released 1.0.1 and confirmed sonnet's API is available again (it had degraded to a core-only third-party review in panels M-Q and was fully unavailable for W-EE; those panels ran on opus+haiku per the 2026-06-23 decision). Full diversity is again the three models opus+sonnet+haiku; a clean panel needs all three. Panel FF is the first full-3-model panel since the restoration \u2014 a post-1.0.1-release confidence check.",
  "package": "stitchgraph",
  "severity_weights": {
    "CRITICAL": 40,
    "HIGH": 10,
    "MEDIUM": 4,
    "LOW": 1,
    "NIT": 0.2
  },
  "available_models": [
    "opus",
    "sonnet",
    "haiku"
  ],
  "tau": 2,
  "coverage_target": 0.85,
  "contract_coverage_estimate": 0.75,
  "known_open_defects": 0,
  "signals": {
    "property_tests": {
      "file": "tests/test_properties.py",
      "needle": "hypothesis"
    },
    "security": {
      "file": "tests/test_safety.py",
      "needles": [
        "read_only",
        "malformed",
        "traversal"
      ]
    },
    "docs": [
      "LIMITATIONS.md",
      "CONTRIBUTING.md",
      "README.md"
    ]
  },
  "panels": [
    {
      "id": "A",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 3,
        "MEDIUM": 3,
        "LOW": 1
      },
      "theme": "symmetry gaps: a liveness rule / ambiguity guard / type preserved in one extractor or resolver but missing in a sibling"
    },
    {
      "id": "B",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 1,
        "MEDIUM": 1,
        "LOW": 1
      },
      "theme": "more symmetry gaps in the *other* siblings: tree-sitter twin of F4 (opus+sonnet converged), html/jsfetch route collapse, .py-only git risk"
    },
    {
      "id": "C",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 1,
        "MEDIUM": 2
      },
      "theme": "deeper surfaces: tree-sitter callback-role twin of _apply_callback_roles, single-arg signal .connect(), SQL CTE phantom table"
    },
    {
      "id": "D",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 1,
        "LOW": 2
      },
      "theme": "incremental/edge surfaces: _resolve_worklist ambiguity (opus+sonnet converged), tree-sitter recursion self-edge, malformed-coverage crash"
    },
    {
      "id": "E",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 1,
        "LOW": 2,
        "NIT": 1
      },
      "theme": "untested language path: C/C++ functions silently dropped (return type read before declarator); plus exported-method over-seeding for Java/C#, INSERT...SELECT read/write label, jsfetch route guard"
    },
    {
      "id": "F",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 1,
        "MEDIUM": 1,
        "LOW": 2
      },
      "theme": "Ruby paren-less calls dropped (all three converged); TS export{} re-export not seeded; trace_path vacuous-ok; SQL multi-statement label"
    },
    {
      "id": "G",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 1,
        "LOW": 2
      },
      "theme": "no HIGH, haiku clean \u2014 metrics/data-side polish: parallel-edge dedup (fan_in/get_matrix), get_matrix cells, ORM relationship() phantom column"
    },
    {
      "id": "H",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "CLEAN \u2014 all three models returned FINDINGS: none; verified every recent fix for regressions, the precision invariant on adversarial input, the envelope contract, and GraphBLAS agreement"
    },
    {
      "id": "I",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 1,
        "LOW": 1
      },
      "theme": "confirmation gate caught a real one: new Foo() constructor calls not edged in JS/TS/C#/C++ (live class flagged dead) \u2014 eight panels missed it; plus read-only global false data_loop. Streak resets."
    },
    {
      "id": "J",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 3
      },
      "theme": "the use-form class, fully exposed: bare-name references not modeled (opus, Python+JS), PHP new and Ruby .new constructors missed by the Panel I fix (sonnet). One general _direct_refs/_direct_names pass now models ALL by-name references, closing the whole class. haiku read these as the documented advisory limitation."
    },
    {
      "id": "K",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 1,
        "MEDIUM": 1
      },
      "theme": "tail of the use-form class + a self-inflicted regression: Python signature type annotations not modeled (opus HIGH); the Panel J _direct_refs id()-skip was ineffective on tree-sitter wrappers, spurious REFERENCES self-loops + callee double-edges inflating fan_in (opus+sonnet converged, MEDIUM). haiku clean."
    },
    {
      "id": "L",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 1,
        "MEDIUM": 2
      },
      "theme": "constructing a class didn't reach its constructor -> constructions in __init__ flagged dead (haiku HIGH); Python twins of Panel K's tree-sitter metric fixes \u2014 CALLS+REFERENCES double-edge (opus) and REFERENCES self-loop (sonnet) \u2014 now fixed language-agnostically at the dedup boundary. (sonnet's slot hit a 500; finding salvaged from transcript and fixed.)"
    },
    {
      "id": "M",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 2,
        "MEDIUM": 3
      },
      "theme": "PHP public class flagged dead (opus HIGH). Third-party 'sonnet' (core-only env) found the blind spot the all-extras agents miss: config loaded from cwd not the indexed root (HIGH); ingest_trace claims success grounding nothing (MEDIUM); edges-table migration + index-ordering on old DBs (MEDIUM); core-only CI red \u2014 unguarded hypothesis/tree-sitter test imports (MEDIUM). haiku clean."
    },
    {
      "id": "N",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 1
      },
      "theme": "opus+haiku CONVERGED: a class/fn used only as a Python parameter default value (def f(x=Strategy)) was flagged dead \u2014 func.args.defaults/kw_defaults weren't walked (the 3rd instance of Python-walks-body-not-signature, after Panel K annotations). _annotation_names -> _signature_ref_names now covers defaults too. Third-party 'sonnet' (core-only) clean, re-confirming Panel M's fixes."
    },
    {
      "id": "O",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 1
      },
      "theme": "lightest panel since clean H: opus+haiku CONVERGED on one narrow MEDIUM \u2014 a metaclass used only via class X(metaclass=Meta) flagged dead (_walk_scope edged child.bases but not child.keywords). Now walks class-def keywords. Third-party 'sonnet' (core-only) clean for the 2nd straight panel."
    },
    {
      "id": "P",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 1
      },
      "theme": "the last un-walked Python scope: references in the class *body* itself (attribute assignments `h = Helper`, dispatch tables `TABLE = {\"a\": handle_a}`, class-level annotations) were never extracted \u2014 _walk_scope edged class bases/keywords/ctors and recursed into method bodies, but never the class body's own statements -> live symbols flagged dead (opus HIGH). Now walks class-body Load names via _direct_names, attributed to the class node (matching tree-sitter, which walks the whole class node). haiku clean; third-party 'sonnet' (core-only) clean for the 3rd straight panel."
    },
    {
      "id": "Q",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "CRITICAL": 1,
        "HIGH": 1,
        "MEDIUM": 1
      },
      "theme": "the confirmation gate caught a CRITICAL: a symbol used only inside a function-LOCAL class/closure was flagged dead \u2014 _def_node never descended into function bodies, so function-local defs were never nodes, yet _walk_scope emitted edges from their phantom qualnames that couldn't reach (opus, Python<->tree-sitter asymmetry, the un-walked NESTED scope after class bodies). _def_node now models nested defs as real nodes + a function->nested containment edge (a local def is live iff its enclosing fn is reachable; fixes decorator-registered @app.command handlers). Third-party 'sonnet' (core-only): public re-exports from __init__ (`from .api import Public`) weren't export roots -> live public API flagged dead (HIGH); version still 0.3.0 not 1.0.0 (MEDIUM, deferred to the manual release step). haiku clean. Streak resets."
    },
    {
      "id": "R",
      "models": [
        "opus",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 2
      },
      "theme": "the metric twin of Panel Q's nested-def fix, in both extractors. opus+sonnet (sonnet partial, stopped on API issues but converged on the class-body variant first): the five Python `_direct_*` own-scope helpers leaked into nested defs \u2014 the driver loop ran rec() on a top-level stmt that was itself a nested def, so the nested def's calls/refs/globals were mis-attributed to the enclosing function (162 spurious CALLS + 40 spurious class->symbol REFERENCES on self; 15 false god-objects -> 11). Panel Q made it observable by giving nested defs real nodes, so the spurious parent edge now double-counts instead of being a dropped phantom. Fixed by skipping a top-level body stmt that is itself a def, in all five helpers (MEDIUM, metric inflation, errs safe). haiku: the tree-sitter SIDE of Panel Q's Python nesting \u2014 function-local defs were created at MODULE scope (`app.ts::helper` not `app.ts::setup.helper`), so two same-named defs merged into one node (corrupting get_callers/callees/impact_of for that name). Reproduced; errs safe for the cardinal invariant (no live-flagged-dead, confirmed by opus too) so downgraded from haiku's CRITICAL to MEDIUM. Fixed symmetrically: nest every def's children under its own qual + emit an enclosing-function->nested containment edge (Python parity, Panel Q), kept live iff the enclosing fn is reachable. sonnet's slot was stopped (API issues this session); diversity 2/3. Streak stays 0."
    },
    {
      "id": "S",
      "models": [
        "opus",
        "haiku"
      ],
      "findings": {
        "CRITICAL": 1
      },
      "theme": "the confirmation gate caught a CRITICAL regression that Panel R itself introduced \u2014 exactly why consecutive clean panels are required. opus: Panel R fixed the nested-def body leak by having each of the five `_direct_*` driver loops `continue` on a top-level stmt that is itself a def. But a def's HEADER expressions \u2014 decorator calls/arguments (`@registry(make_validator())`) and a nested class's base expressions (`class L(get_base())`) \u2014 execute in the ENCLOSING function's scope at definition time, yet were syntactically part of the skipped statement. They aren't recovered elsewhere (`_decorator_edges` edges only the decorator name; `_walk_scope` edges a base only via `_name_of`, None for a Call). So a symbol used only there got zero inbound edges and was flagged dead \u2014 pre-R it erred safe (over-attributed to the enclosing fn), post-R it erred UNSAFE (false dead, the cardinal sin). Fixed with `_def_header_refs`: when the driver hits a nested def, it still walks the def's header (decorators + class bases/keywords) in the enclosing scope while skipping the body \u2014 restoring the dropped enclosing-scope refs without re-introducing the body leak (verified: outer no longer calls a body-only callee; build now reaches make_validator/make_base; self-scan unchanged at 3 advisory). haiku: clean (FINDINGS: none) after 48 adversarial tests verifying the Panel R fixes, the cardinal invariant across Python/TS/JS, metric-dedup, and cross-language parity. diversity 2/2 (opus+haiku full under the updated rule). Streak resets to 0."
    },
    {
      "id": "T",
      "models": [
        "opus",
        "haiku"
      ],
      "findings": {
        "CRITICAL": 2
      },
      "theme": "two more instances of the nested-scope class (Panel Q), in the two remaining nesting hosts \u2014 both CRITICAL false-deads, both pre-existing (not regressions). haiku: a Python def nested in a CONTROL-FLOW block (`if`/`for`/`while`/`try`/`with`/`match`) was never modeled \u2014 `_def_node`/`_walk_scope` walked only a function's *direct* body, so a def in an `if` got no node while `_walk_scope` emitted edges from its qualname (phantom source) -> a symbol used only there flagged dead. opus: the tree-sitter twin one host deeper \u2014 a def nested in a JS/TS ARROW function (`const h = () => { function w(){...} }`, pervasive) was never modeled because the arrow-declarator branch created the node but never recursed into the arrow body nor threaded the containment edge. Rather than wait for panels to find hosts one at a time, did a SYSTEMATIC nesting-host audit: a def can nest only in a function body (Q), class body (P), control-flow block, or function-expression/arrow (lambdas/comprehensions can't hold defs). Fixes: Python `_scope_defs` looks *through* control flow (no qual level added) in `_def_node`/`_walk_scope`/`_iter_funcs`/the module loop; tree-sitter arrow branch now recurses into the arrow body + emits the containment edge. Test matrix pins every host in both extractors (148 tests). Dogfood: find_stale still 3 advisory, 0 holes (precision intact). diversity 2/2. Streak resets to 0; the nesting-host enumeration is now believed complete."
    },
    {
      "id": "U",
      "models": [
        "opus",
        "haiku"
      ],
      "findings": {},
      "theme": "CLEAN \u2014 both opus and haiku returned FINDINGS: none at full diversity (opus+haiku), the first clean panel since the nesting-host audit. This panel also ran a REGRESSION-TEST-QUALITY audit (folded into scope at the maintainer's request after two test-authoring slips in R/S): both models confirmed the Panel Q-T nesting regression tests are NON-VACUOUS \u2014 opus monkeypatched `_scope_defs` back to a pre-fix direct-body-only variant in a /tmp repro and observed 5/5 control-flow tests FAIL (helper flagged dead, nested def not modeled), then pass post-fix; the tests assert on the right symbols, use projects with real entry points so dead-code analysis is meaningful, and have assertions that can fail. Source probing found no still-unmodeled def host (function/class/control-flow/arrow is complete; lambdas/comprehensions can't hold defs), verified the body-leak (R) and header-leak (S) fixes still hold, confirmed no metric inflation from containment edges (_dedup_edges collapses the CALLS+REFERENCES pair, fan_in==1), and checked module-level `try: import` fallback + `TYPE_CHECKING` idioms. Dogfood: find_stale 3 advisory, 0 holes (precision intact). Clean streak 1 of 2."
    },
    {
      "id": "V",
      "models": [
        "opus",
        "haiku"
      ],
      "findings": {},
      "theme": "CLEAN \u2014 the SECOND consecutive full-diversity clean panel; the release gate is now met (gates green AND RRS >= 90 AND clean streak 2). Both opus and haiku returned FINDINGS: none after being steered AWAY from the now-closed nesting class and toward the less-trodden surfaces, so a clean verdict is earned: cross-language resolvers (SQL CTE/UNION/INSERT\u2026SELECT/MERGE, ORM mapped_column/relationship exclusion, events additive-only), the full envelope contract (every operations.py urgency= assignment audited against the provenance ceiling; scan RED requires an EXTRACTED-only certain_live path; find_stale 0.6/INFERRED/needs_review; ingest_trace refuses on zero grounding; no ok=True-with-vacuous-result), persistence/migration (nodes+edges migrate, indexes after; replace_file confirmed wired into no product path), metrics (dedup on the full resolver-inclusive edge list; LIVENESS_RELATIONS excludes QUERIES/READS/WRITES/MAPS_TO; get_matrix N/N+1 bounds), a 2000-random-graph GraphBLAS-vs-pure-Python reachability fuzz (0 mismatches), and precision use-forms (walrus/comprehensions/f-strings/forward-refs/__all__/property/staticmethod). Two non-defect sub-observations recorded, neither flagged as a finding: SQL MERGE labels its target READS not WRITES (recall-only, err-safe, uncommon) and find_holes sets urgency=ORANGE on an empty hole list (cosmetic; ok=True/count=0/contract intact). Dogfood: find_stale 3 advisory, 0 holes. Clean streak 2 of 2 \u2014 RELEASABLE; the maintainer tags 1.0.0 manually."
    },
    {
      "id": "W",
      "models": [
        "opus",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 1
      },
      "theme": "1.0.1 field-fix confirmation. opus+haiku CONVERGED on one err-safe over-match in the just-landed #8 fix: the Rust test-attribute detector matched a raw 'test' substring (any('test' in a for a in attrs)), so #[cfg(feature=\"testing\")] / #[doc=\"...test...\"] wrongly seeded the test role on production code, HIDING genuinely-dead code. Errs safe (over-mark -> keep live -> never a false-dead), so NOT a cardinal-invariant violation and not release-blocking, but real. #7 (grammar-load surfacing) and #9 (impact_of homonym scoping; dotted-name + Foo.bar/XFoo.bar false-resolution probes) both verified correct. Fixed: _is_rust_test_attr matches the attribute PATH (test / *::test / bare cfg(test)/cfg(all(test,...)) with quoted values stripped) + a both-directions regression test."
    },
    {
      "id": "X",
      "models": [
        "opus",
        "haiku"
      ],
      "findings": {},
      "theme": "CLEAN 1.0.1 re-confirmation of the Panel W fix. Both FINDINGS: none. Path-based matcher verified BOTH directions: false-positives gone (#[cfg(feature=\"testing\")], #[doc], #[allow], #[derive], a feature literally named test), no false-negatives (#[test], #[ test ], #[tokio::test]/*::test, #[cfg(test)], #[cfg(all(test, feature=\"x\"))], inner attrs, free-form test-mod names); attribute-bleed resets correctly; core #8 liveness holds (test helper reached by no test + unused production still flag). #7/#9 re-confirmed; dogfood 3 advisory / 0 holes. One err-safe note documented in LIMITATIONS.md (third-party runner macros #[rstest]/#[test_case] under-mark; macro set unenumerable, matching them would re-open the W over-match), not a finding. Per the maintainer's decision, 1.0.1 ships on this clean dual-model confirmation (agreed fix -> confirmation-panel process) rather than re-establishing full streak-2."
    },
    {
      "id": "Y",
      "models": [
        "opus",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 1
      },
      "theme": "Polyglot test-detection review. New generalization (Java/C# @Test/[Fact], PHP #[Test], JS/TS/Ruby call-based test()/it(), _seed_test_classes, module-call rooting) verified clean both directions (annotation allowlist exact; call-based helpers live; dead helpers flagged). One err-safe MEDIUM both models converged on (Panel W class): _is_test_file matched testing/ and specs/ dirs, plausibly production (Go testing, OpenAPI specs), rooting their module calls and hiding dead code. Not cardinal. Fixed by trimming dir set to test/tests/spec/__tests__."
    },
    {
      "id": "Z",
      "models": [
        "opus",
        "haiku"
      ],
      "findings": {
        "CRITICAL": 1
      },
      "theme": "opus caught a CARDINAL haiku missed: a pytest class TestWidget: whose methods are all test_* was flagged dead. Recognized test methods become NodeKind.TEST (not METHOD), so the callback path only rescued classes with a non-test override (setUp); the dominant all-test_* class got no role, no inbound liveness edge, flagged dead (live framework-collected code). Fixed: Python _seed_test_classes (a class with any NodeKind.TEST/test-role member seeded test). Bare class Meta(type) still flags. Streak resets."
    },
    {
      "id": "AA",
      "models": [
        "opus",
        "haiku"
      ],
      "findings": {
        "CRITICAL": 2
      },
      "theme": "Two CARDINAL siblings of Z (same container-live/flagged-dead shape, one level removed; both pre-existing). opus: (1) a test class inheriting all its tests from a custom base (JUnit abstract-base + thin-subclass; pytest inherited tests) had no direct test member, not seeded, flagged; (2) the outer of a nested test class was flagged. Fixed: _seed_test_classes grows the seed set transitively (enclosing chain + subclasses of a test base) in both extractors. Java FQ/args annotations verified handled."
    },
    {
      "id": "BB",
      "models": [
        "opus",
        "haiku"
      ],
      "findings": {
        "CRITICAL": 1,
        "MEDIUM": 1
      },
      "theme": "opus broke the AA fix: the two growth axes ran sequentially, not to a combined fixed point, so a nested+inherited class left its outer flagged (idiomatic pytest grouping class TestApi: class TestV1(_SharedCases): pass; Java nested-extends-abstract-base). Fixed by co-iterating both axes in one while-changed loop (monotonic, terminates) in both extractors. MEDIUM err-safe: tree-sitter resolved bases by name across all languages; fixed by keying (lang, name). Streak resets."
    },
    {
      "id": "CC",
      "models": [
        "opus",
        "haiku"
      ],
      "findings": {
        "CRITICAL": 1,
        "MEDIUM": 1
      },
      "theme": "opus: the Python and tree-sitter is_test_file had drifted (Python checked only the filename, tree-sitter also directories), so a shared test base in tests/conftest.py got no test role and its inheriting subclass was flagged dead (CARDINAL). Fixed by extracting one shared directory-aware is_test_file (_testfile.py) used by both extractors so they cannot drift again. haiku MEDIUM err-safe: #[cfg(not(test))] production code marked a test root (the bare test token sat inside not(...)); fixed by dropping not(...) predicates before the scan. Streak resets."
    },
    {
      "id": "DD",
      "models": [
        "opus",
        "haiku"
      ],
      "findings": {},
      "theme": "CLEAN. Convergence verdict: the test-liveness class is CLOSED for 1.0.1. Both FINDINGS: none. opus confirmed all CC fixes (conftest base + subclass live; full Rust cfg matrix incl. not(test)/all(not(test),...)) and ran a final convergence attack (deep nesting x inheritance, mixins, metaclasses, JUnit @Nested) with no remaining live->dead case. The one residual (a test base in a non-test directory subclassed by an own-method-less test) is realistic-but-rare, precision-safe, needs_review-only, has escape hatches, and closing it generically would regress the Y/W over-match guard, so it is a documented limitation. haiku full-matrix sweep clean. Dogfood 3 advisory / 0 holes; 165 tests."
    },
    {
      "id": "EE",
      "models": [
        "opus",
        "haiku"
      ],
      "findings": {},
      "theme": "CLEAN. Second consecutive full-diversity clean panel -> streak 2, release gate met for 1.0.1. Both FINDINGS: none, fresh independent confirmation (not a rerun of DD). opus ran 11 cross-language scenarios (Python pytest/unittest/inherited/nested/deep-chain, Java JUnit pkg-private abstract base + cross-file inherited base, C#/PHP/JS/Ruby call-based, Rust #[tokio::test], Go cross-file, mixed) -> only genuinely-dead helpers flagged; over-marking bounded (bare metaclass, dead prod, production testing/ & specs/ all flag); #7 silent on grammar-present run (under -W error); #9 candidates + qualified scoping for impact_of/get_callers/get_callees/trace_path; envelope contract intact (find_stale needs_review, conf<=0.6, INFERRED). haiku full sweep clean. Dogfood src/ = 3 advisory, 0 holes; 165 tests."
    },
    {
      "id": "FF",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "CRITICAL": 2
      },
      "theme": "First full-3-model panel after sonnet's restoration (post-1.0.1 confidence check). opus + haiku both clean, but FRESH sonnet found TWO CARDINAL false-deads the opus/haiku pair missed across all of W-EE: (1) JS/TS 'export default Foo;' where Foo is defined earlier in the file (canonical React/Angular/Vue/Node idiom) never set the exported role -> Foo flagged dead (PRE-EXISTING, in 1.0.0/1.0.1; _reexport_names only handled export { X }); (2) Ruby a class used as a call receiver in an RSpec describe/it block (Service.run) got a CALLS edge to the method but no REFERENCES edge to the class -> live class flagged dead (INTRODUCED by 1.0.1 Bug-B _module_calls, which collected calls + bare idents but not constant receivers, unlike _direct_refs). Both confirmed by real-input repro. Validates re-adding sonnet for diversity. Fixed for 1.0.2: _reexport_names handles export default <ident>; _module_calls -> _module_uses also collects name-references (constant/type_identifier/identifier/name) like _direct_refs."
    },
    {
      "id": "GG",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "CRITICAL": 1,
        "MEDIUM": 1
      },
      "theme": "Confirming the FF fixes (1.0.2). Both FF fixes (export default <ident>; _module_uses name-refs) verified correct both directions by all three models; F2 over-marking bounded + dedup intact. opus AND sonnet converged on a CARDINAL sibling of F1 (pre-existing): the whole-module CJS/TS-interop export forms module.exports = X / export = X / module.exports = {A,B} / exports.x = Y are not rooted -> live public export flagged dead. Fixed by extending _reexport_names to those forms. sonnet also found a MEDIUM err-safe over-rooting: _module_uses descended into a `const helper = function(){}` (a named def itself), over-rooting refs in its body even when uncalled; fixed by skipping variable_declarator-with-function-value in _module_uses (consistent with skipping spec.defs). Both fixed + regression-pinned."
    },
    {
      "id": "HH",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "CLEAN (all three FINDINGS: none) confirming the GG export-rooting fixes for 1.0.2. opus: GG fixes correct both directions, member-expression matcher tight (obj.exports/local exports not over-rooted), no metric inflation, export= doesn't false-fire on export const, dogfood 3 advisory/0 holes. haiku: 7 export-form tests + precision + cardinal pass, 85 regression tests green. sonnet: enumerated ALL 19 JS/TS/CJS export forms and verified each -> explicit verdict the export-rooting class is CLOSED; one err-safe non-cardinal note (CJS module.exports= inside a function body over-roots, rare, consistent with documented tradeoffs). First clean panel of the 1.0.2 export-rooting batch (FF/GG found 3 cardinals + 1 medium); streak 1 of 2."
    },
    {
      "id": "II",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "CLEAN (all three FINDINGS: none) \u2014 second consecutive clean panel -> 1.0.2 release gate met (streak 2). Fresh independent confirmation of the whole 1.0.2 candidate, not a rerun: sonnet ran ~30 new export/test-receiver/nesting/cross-language shapes; opus swept 25 export shapes + spawned a non-export sub-agent (SQL/ORM/events/envelope/metrics/persistence/CLI all clean); haiku full matrix. Export-rooting class confirmed CLOSED; over-marking bounded; #7/#9 + four nesting hosts + 1.0.1 polyglot all hold; dedup/metric integrity intact; dogfood src/ 3 advisory / 0 holes; 169 tests. One borderline err-safe observation (module.exports = ns.Wrapped via a locally-built namespace member not rooted) noted for visibility, covered by documented module-level-uses limitation, advisory-only, not a finding."
    },
    {
      "id": "JJ",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "CLEAN (all three FINDINGS: none) confirming the issue-#12 fix (1.0.3): offline-by-default grammar pin (tree-sitter-language-pack>=0.7,<1.0 bundled line) + adaptive _load_grammar (download-and-retry when the pack supports it) + stitchgraph doctor/--strict self-check + [treesitter-download] opt-in. Verified the get_language->_load_grammar swap is behaviour-preserving (byte-identical node/edge/role/provenance graph across all 12 languages; success path makes zero download() calls); all 4 adaptive-load paths (success / fail+download->retry / fail+bundled->#7 warn+skip / download-itself-fails->propagate, never a silent empty graph or false-dead); doctor + --strict exit codes incl. not-installed path; pin bounds correct ([all] stays offline-by-default); no regression (172 tests, FF/nesting cardinal fixes hold); dogfood src/ 3 advisory / 0 holes. Offline operation independently verified earlier in a throwaway venv on bundled 0.13.0 with network OFF. First clean panel of the 1.0.3 batch; streak 1 of 2."
    },
    {
      "id": "KK",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 1,
        "LOW": 1
      },
      "theme": "First full-3-model panel of the 1.0.4 batch (#10 receiver-call demotion, #11 scan provenance propagation, #15 LIMITATIONS --precise wording), reviewed at f69df0b/b2873df. opus and haiku: FINDINGS: none \u2014 verified the cardinal invariant directly (weight stays 1.0, no new false-dead; #10 only relabels provenance), no scan crash/mislabel, jedi additive matches #15 wording. opus raised a non-blocking NOTE that became a real consistency fix (scored MEDIUM): #10 was tree-sitter-only \u2014 the Python ast extractor still labelled an unknown-receiver `x.save()` matched name-only to a lone project `save` as EXTRACTED (the same over-claim; a Python<->tree-sitter asymmetry, this project's standing defect class). Fixed by mirroring the demotion in _call_edge/_ref_edges (is_method param): scope-aware self./local-typed calls stay EXTRACTED, only the unknown-receiver fallback demotes. sonnet found a LOW 1.0.4 regression: C# `new MyApp.Widget()` parses with a `qualified_name` callee node (in _RECEIVER_CALLEE), so the namespace-qualified CONSTRUCTOR was wrongly demoted to INFERRED (cardinal-safe, weight 1.0, but a provenance-accuracy bug that could also under-count confident edges in scan). Fixed generally: constructor call-fields (\"constructor\"/\"type\") force is_method=False in _callee, so any qualified ctor stays EXTRACTED; Rust `Widget::new()` / Ruby `Widget.new` are real method-call nodes and keep their documented INFERRED. Both fixes regression-pinned (pytest 177). Not a clean panel \u2014 surfaced fixable findings; streak resets, fresh confirmation (LL) follows."
    },
    {
      "id": "LL",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "CLEAN (all three FINDINGS: none) \u2014 first clean panel of the 1.0.4 batch; fresh full-3-model confirmation on the final tree (a68f827) after the KK fixes. opus: end-to-end cardinal proof that liveness is provenance-agnostic \u2014 a symbol reached ONLY via a #10 INFERRED receiver edge is NOT flagged dead while a genuinely unused sibling is; verified the constructor call-fields (\"constructor\"/\"type\") bind EXCLUSIVELY to constructor node types across the LangSpec map (so no real method call is wrongly kept EXTRACTED), and that --precise is genuinely additive (_dedup_edges only collapses same (src,relation,dst_id); AMBIGUOUS siblings preserved). haiku: full suite 177 + dogfood 3 advisory/0 holes + scan on cycle/empty no crash. sonnet: re-ran the per-language constructor matrix (C#/Java/JS/TS/C++/Rust/Ruby/PHP \u2014 qualified & simple ctors EXTRACTED, method calls INFERRED, Rust ::new / Ruby .new documented INFERRED) and adversarial _resolve_member defeat attempts (chained calls, self.x.m(), annotated params, reassigned locals) \u2014 all correct. Streak 1 of 2."
    },
    {
      "id": "MM",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "CLEAN (all three FINDINGS: none) \u2014 SECOND consecutive clean full-3-model panel; the 1.0.4 release gate is met (streak 2). Fresh independent confirmation, not a rerun. opus: proved end-to-end that the only provenance consumers are reporting/ranking (scan RED/ORANGE ceiling, #11 capping, trace_path confidence) \u2014 none feed reachable_from/find_stale, so #10's INFERRED relabel cannot drop liveness; _CONSTRUCTOR_FIELDS maps exclusively to constructor callee fields. sonnet: 12 #11 boundary scenarios (mixed EXTRACTED/AMBIGUOUS cycles straddling frac<0.5, god_object c_in/c_out boundaries, self-loop, god-object-in-cycle, REFERENCES-only no-cycle) \u2014 no genuine EXTRACTED finding ever wrongly capped GREEN; 7 downstream-op checks (trace_path/impact_of/find_stale/orient/stub-ceiling all provenance-agnostic for liveness); 9 cross-extractor parity checks (KK asymmetry stays closed). haiku: full suite 177, dogfood 3 advisory/0 holes, the four spec repros, empty/single-file no crash. No false-dead, no metric inflation, no envelope-contract violation, no crash. 1.0.4 is RELEASABLE; awaits the maintainer's manual v1.0.4 tag."
    },
    {
      "id": "NN",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "LOW": 2
      },
      "theme": "1.0.5 #18/#19 batch. opus/haiku clean; sonnet found report passed repo='.' to risk (same #18 cwd bug -> blank/skipped risk section from foreign cwd) + phantom structure_smells() in design.md \u00a79. Both fixed."
    },
    {
      "id": "OO",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "LOW": 1
      },
      "theme": "1.0.5 confirm. 5/6 clean; sonnet: design.md \u00a79 still listed type_at (unbuilt LSP-roadmap op) \u2014 my CHANGELOG over-claimed '\u00a79 matches the operation set'. Fixed (type_at removed from \u00a79)."
    },
    {
      "id": "PP",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "LOW": 2
      },
      "theme": "1.0.5 confirm. 5/6 clean; sonnet: design.md \u00a79 still had trace_path's non-existent relations? arg; and stitchgraph.__version__ hardcoded '1.0.3' (stale 2 releases). Fixed: \u00a79 scrubbed + __version__ derived from importlib.metadata."
    },
    {
      "id": "QQ",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 1
      },
      "theme": "1.0.5 confirm (parallel with RR on 61339f3). 5/6 clean; sonnet (MEDIUM): risk() 'no git history' path used refuse(result={}) -> ok=True with vacuous result, so report rendered a blank Risk section (the one remaining ok=True-empty the 1.0.0 audit claimed absent). Fixed: drop result={} -> ok=False -> report '(skipped)'."
    },
    {
      "id": "RR",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 1
      },
      "theme": "1.0.5 confirm (parallel with QQ). Independently corroborated the same risk empty-churn vacuous-ok=True envelope bug (LOW/MEDIUM). Fixed with QQ."
    },
    {
      "id": "SS",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 1
      },
      "theme": "1.0.5 confirm (parallel with TT on 61339f3 after empty-churn fix). 5/6 clean; opus (MEDIUM): the empty-churn fix closed only one door \u2014 when churn is non-empty but all files have zero centrality and no hidden coupling, risk still returned ok=True with empty hotspots/hidden -> blank report section. Fixed."
    },
    {
      "id": "TT",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 1
      },
      "theme": "1.0.5 confirm (parallel with SS). opus+sonnet independently found the same empty-centrality blank-section. Consensus fix (codebase-consistent): risk keeps ok=True structured-empty (like find_stale []), and report renders '- (no risk hotspots or hidden coupling found)' so the section is populated-or-skipped, never blank. Fixed."
    },
    {
      "id": "UU",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "CLEAN (all three FINDINGS: none) on the final tree e5785fa. Full envelope-contract + cardinal + crash + risk/report empty-state sweep; the risk/report empty class is fully closed (refuse->'(skipped)' when risk can't run; ok=True structured-empty->'(none)' when it ran and found nothing). _root live_stub RED confirmed a genuine advisory (intentionally-empty Typer callback), not a false positive. Streak 1 of 2."
    },
    {
      "id": "VV",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "CLEAN (all three FINDINGS: none) \u2014 second consecutive clean full-3-model panel on e5785fa; 1.0.5 gate met (streak 2). Independent sweep: envelope contract across 15 ops, MCP end-to-end (risk path-omitted schema valid), multi-language cardinal repos (metaclass/class-body/nested-def/arrow-in-arrow/forward-ref/cross-language route+fetch \u2014 none false-dead), adversarial risk/report (worktree/submodule/spaces/relative-db/deleted-root) all clean. Dogfood 3 advisory / 0 holes."
    },
    {
      "id": "R33",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "CRITICAL": 2,
        "HIGH": 3
      },
      "theme": "1.0.6 full-diversity (opus\u00d72\u00b7sonnet\u00d72\u00b7haiku\u00d72). 5 blockers fixed at root: Ruby/PHP module-level script rooting (CARDINAL), replace_file erasing runtime role + has_runtime inflation (CARDINAL), empty-string ignore glob crash, impact_of provenance/confidence inflation, get_matrix/summarize_subsystem scope-prefix bleed. Each owned by a regression test."
    },
    {
      "id": "R34",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 3
      },
      "theme": "1.0.6. 3 blockers: trace_path provenance inflation (provenance-demotion column missed this sibling), cross-language bare-name widening on incremental path (same_lang SQL filter + polyglot oracle), runtime _by_suffix path-boundary inflation. Plus broken-config-test fix + mutation green-baseline guard."
    },
    {
      "id": "R35",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "CRITICAL": 2,
        "HIGH": 2
      },
      "theme": "1.0.6. 2 cardinals (Go package-directory var-initializer rooting; Express method-reference route handler) + 2 inflations (coverage bool-as-int line; cross-root coverage suffix exact-match-first). Each owned."
    },
    {
      "id": "R36",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "CRITICAL": 1
      },
      "theme": "1.0.6. 1 cardinal: C++ translation-unit static-init liveness (reachability-driven _live_set fixpoint). Envelope non-finite clamp hardening (latent). 4/6 reviewers clean, 1 invalid."
    },
    {
      "id": "R37",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "CRITICAL": 2
      },
      "theme": "1.0.6. 2 cardinals: module/symbol id-collision module-load rooting (JS+Python); cross-file re-export on incremental replace_file (exported_ids exact-convergence protocol + oracle). _plain non-finite drop. 3/6 clean, 1 invalid, 1 non-blocking."
    },
    {
      "id": "R38",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "1.0.6 CLEAN \u2014 first 3-layer-clean round. Zero valid blockers across all 6 reviewers (extraction/resolution, operations/metrics, GraphBLAS parity, incremental+trace, hostile inputs, full CLI). 1 finding invalid (SUBMITS_TO premise/direction), 1 latent non-blocking meta/_plain consistency item closed. Panel clean + oracle suite green (348 tests) + mutation clean (envelope 17/17, config 4/4). Streak 1 of 2."
    },
    {
      "id": "R39",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "1.0.6 CLEAN \u2014 SECOND consecutive 3-layer-clean round (gate met, streak 2). All 6 reviewers FINDINGS:0 across extraction/resolution (deep multi-language syntax, cross-language isolation), operations/metrics/GraphBLAS parity, incremental+trace long sequences, hostile inputs/adapters/corrupt-DB, full CLI sweeps. Panel clean + oracle suite green (348 tests) + mutation clean (envelope 17/17, config 4/4). Dogfood exact."
    },
    {
      "id": "R40",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 2,
        "MEDIUM": 1
      },
      "theme": "1.0.7 fix-panel round 1: R40A script-class over-root (Class.method CLI target rooting its class's whole public surface, HIGH); R40C member-assign-in-dead-function rooted (HIGH); R40B comment dropping a JS/TS @decorator marker (MEDIUM). All over-rooting/recall, cardinal-safe; fixed root-cause + owning tests."
    },
    {
      "id": "R41",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 1
      },
      "theme": "1.0.7 round 2: R41A \u2014 the R40B comment-skip missed Rust line_comment/block_comment, so #[test]+comment+fn dropped the test marker (cardinal false-dead); fixed. Plus 2 documented cardinal-safe tradeoffs (flat-name export-collision over-rooting; cosmetic @Controller export-class role)."
    },
    {
      "id": "R42",
      "models": [
        "opus",
        "sonnet"
      ],
      "findings": {},
      "theme": "1.0.7 round 3: CLEAN \u2014 both reviewers no cardinal/crash; verified R40/R41 fixes correct + complete."
    },
    {
      "id": "R43",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 1
      },
      "theme": "1.0.7 round 4: R42A \u2014 namespace-package src-layout (no __init__.py under src/) not detected, so absolute imports stayed external and module-load-only-live code was flagged dead (cardinal); fixed (detect any src/<pkg>/*.py). Cross-feature + fuzz reviewers clean (0 crashes/69 fuzz inputs). Fix reset the streak."
    },
    {
      "id": "R44",
      "models": [
        "opus",
        "sonnet"
      ],
      "findings": {},
      "theme": "1.0.7 round 5: CLEAN \u2014 R42A broadening verified cardinal-safe (real module wins qualname key via direct-assign vs setdefault alias; only adds edges); 20+ real-world shapes + relative-imports/Django/tsx/cpp-EXPORT_SYMBOL/multi-src all clean."
    },
    {
      "id": "R45",
      "models": [
        "opus",
        "sonnet"
      ],
      "findings": {},
      "theme": "1.0.7 round 6: CLEAN \u2014 SECOND consecutive clean round (gate met, streak 2). Final cardinal+crash sweep over 17+ boundary repros; suite 373 green; one JS top-level-fn flag verified pre-existing on the 1.0.6 baseline, not introduced."
    },
    {
      "id": "R46",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "HIGH": 1
      },
      "theme": "1.0.7 FULL-DIVERSITY round (opus+sonnet+haiku): R46A \u2014 a module-scope member-assigned CLASS (exports.X = class {}) took role 'callback' not 'exported', so _seed_exported_class_methods never rescued its public methods -> class live but its methods + their private callees flagged dead (inverse-cardinal). Fixed: member-assigned class takes 'exported'. Fuzz seat: 0 crashes/15 inputs. Net-new feature-9 regression; streak reset."
    },
    {
      "id": "R47",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "1.0.7 FULL-DIVERSITY clean round 1 (opus+sonnet+haiku). opus verified R46A correct+complete (member-assigned class extends/nested-in-dead-fn/underscore-only/inverse-cardinal chain); sonnet 8 real shapes; haiku 15 fuzz inputs (0 crashes). No cardinal/crash."
    },
    {
      "id": "R48",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "1.0.7 FULL-DIVERSITY clean round 2 (gate met, streak 2). opus high-distrust boundary sweep; sonnet 14 real-world apps (Flask/Django/NestJS/Ruby/C-EXPORT_SYMBOL/monorepo); haiku 11 robustness incl all-features-combined + 1000-file repo. No cardinal/crash. Suite 374, oracles 23, mutation 17/17, dogfood find_stale=1."
    },
    {
      "id": "R49",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "2.0.0 streaming indexer \u2014 FULL-DIVERSITY clean round 1 (first clean run; an earlier R49 attempt was discarded as contaminated \u2014 panels had read operations.py while the mutation tester was rewriting it in place). opus: fuzzed _DefInfo/precompute (Rust trait-impl, C++ out-of-line, iface .type), cache_asts re-parse, 59 non-adjacent same-src runs, double-reindex, mixed-lang stems, 260 polyglot fuzz trials \u2014 all byte-identical incl weight/provenance/name_based. sonnet: 15 adversarial scripts (nested scopes, cross-group dedup, AUTO path, resolver edges, 30-class fanout, mixed C/C++/Py) \u2014 identical. haiku: flush/threshold/:memory:/meta/exception-safety \u2014 CLEAN. name_based first-seen-vs-OR is structural-safe (mixed pairs share src -> sink collapses pre-store; 0 affected rows on self-source) and now pinned by the oracle. Mutation _dedup_edges/_auto_stream 17/17 killed; suite 388; oracles incl weight/provenance/name_based."
    },
    {
      "id": "R50",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 1
      },
      "theme": "2.0.0 streaming \u2014 full-diversity round 2. opus found a REAL byte-identity break: reindex(precise=True, streaming=True) diverged from streaming=False on name_based (jedi's precise edge + the extractor's name-based edge to the same (src,rel,dst_id) land in different sink groups -> store ORs name_based while in-memory _dedup_edges kept the precise survivor's flag). haiku + sonnet (15min/80 tools: huge-file>_BATCH, many-tiny-files, holes, syntax-errors, symlink loops, reindex-into-same-store, tiebreak) CLEAN. FIX: _dedup_edges now ORs name_based onto the survivor (the store's R23A rule); pure-precise groups keep False (R22A). Pinned by a precise=True jedi oracle case + unit tests; mutation now 20/20. Streak resets -> R51/R52."
    },
    {
      "id": "R51",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "2.0.0 streaming \u2014 FULL-DIVERSITY clean round 1 of the post-R50-fix streak. Verified the name_based-OR fix under precise=True (jedi 0.20): opus 9-field differential on jedi-outside-candidates, 2/4/5-way homonyms, lower-weight precise, REFERENCES-vs-CALLS, declared-type, self/cls + multi-level override, propagate_overrides equal-weight collisions, polyglot, full src/ tree \u2014 byte-identical. sonnet 14 fuzz scripts / 60+ combos incl 10-class max-ambiguity, non-contiguous same-src, last-group jedi join \u2014 identical. haiku scrutinized the OR loop (no shared-object/leak), meta equality \u2014 CLEAN. All 3 CLEAN; mutation 20/20; suite 391."
    },
    {
      "id": "R52",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "2.0.0 streaming \u2014 FULL-DIVERSITY clean round 2 (streak 2, gate met). Release confirmation. opus: AUTO threshold boundary (streaming=None == forced), store residue (stream->full->stream == fresh full), CLI/MCP tri-state round-trip, resolver-only + protocol-dunder/override + jedi-homonym non-adjacent overlap, ignore-globs \u2014 all byte-identical incl location/source/name_based + find_stale/holes/meta. sonnet: resolver-heavy (Flask/ORM/fetch) + precise=True + reindex-twice fuzzing \u2014 identical. haiku: CLI/MCP surface + docs-accuracy audit (12x, threshold 2000, on-disk caveat) + meta equality \u2014 CLEAN. All 3 CLEAN. Mutation _dedup_edges/_auto_stream 20/20; suite 391; oracles incl streaming==full (weight/provenance/name_based) + precise=True jedi."
    },
    {
      "id": "R53",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "2.0.1 PHP callable-array fix \u2014 FULL-DIVERSITY clean round 1. The fix recognizes [$this,'method'] array callables (usort/preg_replace_callback) -> REFERENCES. opus: append-only/cardinal-safe verified, PHP-only gating, byte-identity across 9 edge fields, edge cases (keyed/3-elem/non-string/nested) all over-root-only. sonnet: 263 non-PHP regressions + edge-case probes, full vs streaming identical. haiku: crash-safety on malformed arrays, quote-stripping, PHP-only flag. All 3 CLEAN. mutation _php_callable_names 5/5 killed; suite 392; Magento Framework 39->30 PHP dead."
    },
    {
      "id": "R54",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 1
      },
      "theme": "2.0.1 PHP fix \u2014 round 2. CODE unanimously clean at real-Magento scale: opus (full==streaming 2104n/17618e, find_stale 8->6 shrank only, attribution correct, precise clean), sonnet (cross-file inheritance target live, typo->0 holes, monotonic, 21586n/1.5Me byte-identical). haiku found 1 MEDIUM: stale DOC \u2014 CHANGELOG + RELEASE_NOTES_v2.0.1 still claimed 'Class::method' string callables are handled, but that branch was dropped in the mutation-cleanup simplification (redundant: string-static targets are public/exported). FIX: corrected both docs to array callables only. Streak resets -> R55/R56."
    },
    {
      "id": "R55",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 1,
        "LOW": 1
      },
      "theme": "2.0.1 PHP fix \u2014 round 1 of fresh streak. CODE unanimously clean (opus+sonnet+haiku: byte-identical, cardinal-safe, PHP-only, suite 392). But the R54 doc fix was INCOMPLETE: CHANGELOG INTRO (not just the Fixed bullet) still claimed 'Class::method' string callables were flagged-dead-and-fixed (MEDIUM, self-contradictory with the same entry's 'needs no handling' note), and two LangSpec in-code comments still listed 'Class::method' as handled (LOW). FIX: grep-verified purge of ALL stale 'handled' claims across CHANGELOG + comments; code/docs/comments now uniformly describe only the [$this,'method'] array form. Streak resets -> R56/R57."
    },
    {
      "id": "R56",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "2.0.1 PHP fix \u2014 FULL-DIVERSITY clean round 1 (post doc-purge). All 3 CLEAN. opus: verified NO remaining 'string callable handled' claim anywhere (CHANGELOG intro+body, RELEASE_NOTES, both LangSpec comments, the docstring) + byte-identity re-confirmed (sha-equal full vs streaming). sonnet: array-callback fuzz (protected/private/cross-file/typo) full==streaming, doc audit clean. haiku: closure grep clean, version 2.0.1, regression + oracle green. Code clean since R53; doc nits fully resolved."
    },
    {
      "id": "R57",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "2.0.1 PHP fix \u2014 FULL-DIVERSITY clean round 2 (streak 2, gate met, RELEASABLE). All 3 CLEAN. opus fresh 41-file PHP repo (every callable position, nested, multibyte, long array() form, trailing comma, precise=True) \u2014 byte-identical, cardinal-safe; surfaced two PRE-EXISTING recall gaps (bare-string function callable usort($x,'fn'); module-scope array callable) \u2014 not regressions, not overclaimed, now documented in LIMITATIONS. sonnet fresh fuzz (default-arg/property/nested callables) full==streaming. haiku closure audit clean. Mutation 5/5; suite 392."
    },
    {
      "id": "R58",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {
        "MEDIUM": 1,
        "LOW": 1
      },
      "theme": "2.1.0 streaming-reachability + SQL-prose \u2014 round 1. opus: differential parent-vs-HEAD on 400n/2000e (both GraphBLAS + pure-Python paths) byte-identical for fan_in/fan_out/reachable/reverse/SCC/best_path/find_stale/impact; Relation.value==name so the string membership switch is exact. Found LOW: iter_resolved bypassed _row_to_edge's corrupt-row drop -> a corrupt relation could reach best_path(relations=None). sonnet: CLEAN. haiku: MEDIUM \u2014 SQL structural regex still let prose 'Select items from the list' parse to a phantom db::the. FIXES: iter_resolved skips invalid relations + coerces non-finite weight; SQL resolver drops English function-word table names (_STOP_TABLES). Streak resets -> R59/R60. Mutation reach 16/16 + sql 3/3; suite 403."
    },
    {
      "id": "R59",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "2.1.0 \u2014 FULL-DIVERSITY clean round 1 (post-R58-fix). opus: differential on 3 real indexes \u2014 iter_resolved corrupt-skip/weight-coerce are strict no-ops on normal indexes (byte-identical find_stale/fan_in/impact), _STOP_TABLES drops no plausible real table. sonnet: corrupt-row + real-SQL-vs-prose verified. haiku: confirmed db::the phantom gone, orders resolves; empty/:memory:/corrupt edge cases clean. All 3 CLEAN. Tidied 2 NITs (stale test docstring, redundant import math)."
    },
    {
      "id": "R60",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "2.1.0 \u2014 FULL-DIVERSITY clean round 2 (streak 2, gate met, RELEASABLE). opus: full differential parent-vs-HEAD on 400n/3000e (GraphBLAS + pure-Python) \u2014 reachable/reverse/fan_in/fan_out/find_stale byte-identical; docs match mechanism (structural regex + _STOP_TABLES, not a signal gate); flagged a stale v2.0.0 LIMITATIONS line (streaming 'opt-in') now corrected to AUTO-default. sonnet: two-layer SQL defense + docs verified. haiku: all 4 invariants, 403 tests + 27 oracles green. All 3 CLEAN."
    },
    {
      "id": "R62",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "2.1.1 Ruby operator-method fix \u2014 FULL-DIVERSITY clean round 1. opus: the shared `operator` capture in _trailing_id never reaches a node/edge-minting path in C++/C# (bare keyword leaf), so zero cross-language regression; setters (type `setter`) handled separately; Ruby-scoped rooting cardinal-safe. sonnet: 24 operator forms classified right; bodies' callees ([]=/<=>/==/<</+@) live, dead normals + their callees still stale; polyglot full==streaming; grape ValueArray.initialize no longer stale. haiku: version 2.1.1, docs accurate, no bogus operator nodes in any language. All 3 CLEAN; mutation _is_ruby_operator_method 3/3; suite 405."
    },
    {
      "id": "R63",
      "models": [
        "opus",
        "sonnet",
        "haiku"
      ],
      "findings": {},
      "theme": "2.1.1 Ruby operator-method fix \u2014 FULL-DIVERSITY clean round 2 (streak 2, gate met, RELEASABLE). Fresh angles: backtick method, unary -@/+@, module-level operator, operator-calls-operator, def self.[] singleton operators, spec-file operators, cross-file homonyms, deeply-nested modules \u2014 all live, no crash. Polyglot Ruby+C++ full==streaming byte-identical. No Go/JS over-rooting. Version 2.1.1, docs accurate. All 3 CLEAN."
    }
  ]
}